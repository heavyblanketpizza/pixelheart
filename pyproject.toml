[project]
name = "pixelheart"
version = "0.1.0"
description = "Offline Stardew Valley NPC authoring and export tools."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.3.0,<13",
    "pyside6>=6.10,<7",
]

[tool.uv]
required-version = ">=0.12.16"
package = false
